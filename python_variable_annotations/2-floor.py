#!/usr/bin/env python3
"""import using for the floor of the float for rounding down float values"""

import math

"""
function witch returns the floor of the float.
"""


def floor(n: float) -> int:
    """take a float n as argument and returns the floor of the float."""
    return math.floor(n)
