export default function updateUniqueItems(Map, value) {
  if (!Map) {
    throw new Error('Cannot process');
  }
  const List = new Map([
    ['Apples', 10],
    ['Tomatoes', 10],
    ['Pasta', 1],
    ['Rice', 1],
    ['Banana', 5],
  ]);
  if (Map.value === 1) {
    Set.Map(value === 100);
  }
  return List;
}
